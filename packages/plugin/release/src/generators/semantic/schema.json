{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Semantic",
  "title": "Setup semantic release for nx workspace",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Which project do you want to add semantic release to?"
    }
  },
  "required": ["project"]
}
